# 게임 동작 문제 해결 요약

## ✅ 수정 완료된 항목

### 1. QuizManager.cs - Null 참조 방지
- ✅ `PlayerStatusManager.Instance` null 체크 추가 (101, 107줄)
- ✅ `feedbackText` null 체크 추가
- ✅ `quizzes` 배열 null 체크 추가
- ✅ `optionButtons` 배열 null 체크 추가
- ✅ `backgroundImage`, `questionText` null 체크 추가
- ✅ `fadeController` null 체크 추가 (146줄)
- ✅ 버튼의 `Text` 컴포넌트 null 체크 추가
- ✅ 씬 로드 전 씬 존재 여부 확인 추가

### 2. FadeController.cs - Null 참조 방지
- ✅ `fadeImage` null 체크 추가 (FadeOut, FadeIn 메서드)

## 🔍 Unity에서 확인해야 할 사항

### 1. Unity Console 확인 (가장 중요!)
Play 버튼을 누르고 Console 창을 확인하세요:
- **빨간 에러 메시지**가 있는지 확인
- 에러 메시지에 나온 **스크립트 이름과 줄 번호** 확인
- 에러 메시지 내용을 읽어보세요

### 2. Inspector에서 참조 확인
`QuizManager`가 붙은 GameObject를 선택하고 Inspector에서 확인:

#### 필수 확인 사항:
- [ ] **Background Image** - Image 컴포넌트가 할당되었는지
- [ ] **Question Text** - Text 컴포넌트가 할당되었는지
- [ ] **Option Buttons** - 배열 크기가 3인지, 각 버튼이 할당되었는지
- [ ] **Feedback Text** - Text 컴포넌트가 할당되었는지
- [ ] **Health Fill** - Image 컴포넌트가 할당되었는지
- [ ] **Quizzes** - 배열에 퀴즈 데이터가 최소 1개 이상 있는지
  - 각 퀴즈에 question, options(3개), correctIndex가 설정되어 있는지
- [ ] **Fade Controller** - FadeController 컴포넌트가 할당되었는지

### 3. 씬 구조 확인
- [ ] **PlayerStatusManager** GameObject가 씬에 있는지 확인
  - Hierarchy 창에서 찾아보세요
  - 없다면 새로 만들어서 `PlayerStatusManager` 스크립트를 붙여주세요
- [ ] **FadeController**가 씬에 있는지 확인
  - Fade Image(검은색 Image)가 있어야 합니다

### 4. Build Settings 확인
- [ ] File > Build Settings 열기
- [ ] `Outdoor_Quiz` 씬이 목록에 있는지 확인
- [ ] 없으면 씬을 열고 "Add Open Scenes" 버튼 클릭

## 🚨 가장 흔한 문제

### 문제 1: Inspector에서 참조가 할당되지 않음
**증상:** 게임 실행 시 아무것도 안 보이거나, 버튼 클릭이 안 됨

**해결:**
1. QuizManager GameObject 선택
2. Inspector에서 모든 필드 확인
3. null로 표시된 항목에 적절한 GameObject/컴포넌트 할당

### 문제 2: PlayerStatusManager가 씬에 없음
**증상:** Console에 "PlayerStatusManager.Instance가 null입니다" 경고 메시지

**해결:**
1. Hierarchy에서 빈 GameObject 생성
2. 이름을 "PlayerStatusManager"로 변경
3. PlayerStatusManager 스크립트를 붙임
4. DontDestroyOnLoad가 자동으로 적용됨

### 문제 3: Quizzes 배열이 비어있음
**증상:** Console에 "quizzes 배열이 비어있거나 할당되지 않았습니다" 에러

**해결:**
1. QuizManager Inspector에서 Quizzes 배열 크기 설정 (예: 3)
2. 각 퀴즈에 다음을 설정:
   - Question: 질문 텍스트
   - Options: 3개의 선택지 텍스트
   - Correct Index: 정답 인덱스 (0, 1, 2 중 하나)
   - Background: 배경 이미지 (선택사항)
   - Damage On Wrong: 틀렸을 때 감소할 체력 (기본 10)

### 문제 4: Outdoor_Ending 씬이 없음
**증상:** 퀴즈를 모두 완료하거나 체력이 0이 되면 에러 발생

**해결:**
1. EndingManager가 있는 씬 생성
2. 씬 이름을 "Outdoor_Ending"으로 저장
3. File > Build Settings에서 "Add Open Scenes" 클릭
4. 또는 코드에서 다른 씬 이름으로 변경

## 📝 다음 단계

1. **Unity 에디터 열기**
2. **Play 버튼 클릭**
3. **Console 창 확인** (Window > General > Console)
4. **에러 메시지가 있으면 확인**
5. **위의 체크리스트에 따라 수정**

## 💡 도움이 필요하면

Unity Console에 나온 정확한 에러 메시지를 알려주시면 더 정확한 해결책을 제시할 수 있습니다!

### 에러 메시지 예시:
- "NullReferenceException: Object reference not set to an instance of an object"
- "The referenced script on this Behaviour is missing!"
- "Scene 'Outdoor_Ending' couldn't be loaded because it has not been added to the build settings"

이런 메시지들을 복사해서 알려주시면 바로 해결할 수 있습니다.

