# 게임 동작 문제 디버깅 체크리스트

## 🔴 가장 흔한 문제들 (우선 확인)

### 1. NullReferenceException (가장 흔함)
**QuizManager.cs에서 확인할 것들:**
- [ ] `PlayerStatusManager.Instance`가 null이 아닌지 확인
  - 씬에 `PlayerStatusManager`가 있는 GameObject가 있는지 확인
  - `PlayerStatusManager` 스크립트가 씬의 GameObject에 붙어있는지 확인
  
- [ ] UI 참조들이 모두 할당되었는지 확인 (Inspector에서)
  - `backgroundImage` 
  - `questionText`
  - `optionButtons[]` (배열 크기와 각 요소)
  - `feedbackText`
  - `healthFill`
  - `fadeController`
  
- [ ] `quizzes` 배열이 할당되고 데이터가 들어있는지 확인

### 2. FadeController 문제
- [ ] `FadeController`의 `fadeImage`가 할당되었는지 확인
- [ ] `QuizManager`의 `fadeController` 참조가 할당되었는지 확인

### 3. 씬 설정 문제
- [ ] `Outdoor_Quiz.unity` 씬이 Build Settings에 추가되어 있는지 확인
  - File > Build Settings > Add Open Scenes
- [ ] `Outdoor_Ending` 씬이 존재하고 Build Settings에 추가되어 있는지 확인
  - 현재는 씬이 없을 수 있음 (에러 발생 가능)

## 🟡 코드 수정이 필요한 부분

### QuizManager.cs - OnAnswer 메서드 (98-116줄)
```csharp
else
{
    // ❌ 문제: PlayerStatusManager.Instance가 null일 수 있음
    PlayerStatusManager.Instance.LoseHealth(q.damageOnWrong);
    // ...
    if (PlayerStatusManager.Instance.health <= 0)
    // ...
}
```

**수정 방법:**
```csharp
else
{
    if (PlayerStatusManager.Instance != null)
    {
        PlayerStatusManager.Instance.LoseHealth(q.damageOnWrong);
        UpdateHealthBar();
        if (feedbackText != null)
            feedbackText.text = "틀렸어요!";
        
        if (PlayerStatusManager.Instance.health <= 0)
        {
            StartCoroutine(GoToEnding());
            return;
        }
    }
    else
    {
        Debug.LogWarning("PlayerStatusManager.Instance가 null입니다!");
        if (feedbackText != null)
            feedbackText.text = "틀렸어요!";
    }
    
    StartCoroutine(AllowInputAfterDelay(0.8f));
}
```

### QuizManager.cs - ShowQuiz 메서드
- `quizzes` 배열 null 체크 추가 필요
- UI 요소들 null 체크 추가 필요

### QuizManager.cs - GoToEnding 메서드 (143-149줄)
```csharp
// ❌ 문제: fadeController가 null일 수 있고, 씬이 없을 수 있음
yield return StartCoroutine(fadeController.FadeOut(0.4f));
SceneManager.LoadScene("Outdoor_Ending");
```

**수정 방법:**
```csharp
if (fadeController != null && fadeController.fadeImage != null)
{
    yield return StartCoroutine(fadeController.FadeOut(0.4f));
}

if (Application.CanStreamedLevelBeLoaded("Outdoor_Ending"))
{
    SceneManager.LoadScene("Outdoor_Ending");
}
else
{
    Debug.LogError("Outdoor_Ending 씬을 찾을 수 없습니다!");
}
```

## 🔍 Unity에서 확인할 것들

### 1. Unity Console 창 확인
- Play 버튼을 누르고 Console 창에서 빨간 에러 메시지 확인
- 어떤 스크립트의 몇 번째 줄에서 에러가 나는지 확인

### 2. Inspector에서 확인
- `QuizManager`가 붙은 GameObject 선택
- Inspector에서 모든 Public 필드가 할당되었는지 확인:
  - `Background Image`
  - `Question Text`
  - `Option Buttons` (배열 크기가 3인지, 각 버튼이 할당되었는지)
  - `Feedback Text`
  - `Health Fill`
  - `Quizzes` (배열에 퀴즈 데이터가 있는지)
  - `Fade Controller`

### 3. 씬 구조 확인
- Hierarchy에서 `PlayerStatusManager`가 있는지 확인
- `PlayerStatusManager` GameObject에 스크립트가 붙어있는지 확인

### 4. Build Settings 확인
- File > Build Settings 열기
- `Outdoor_Quiz` 씬이 목록에 있는지 확인
- 없으면 "Add Open Scenes" 클릭

## 📝 테스트 순서

1. **Unity 콘솔 확인**
   - Play 버튼 클릭
   - 빨간 에러 메시지 확인
   - 에러 메시지의 스크립트 이름과 줄 번호 확인

2. **Inspector 확인**
   - QuizManager GameObject 선택
   - 모든 필드가 할당되었는지 확인
   - null로 표시된 항목이 있는지 확인

3. **씬 구조 확인**
   - PlayerStatusManager가 씬에 있는지 확인
   - 필요한 UI 요소들이 모두 있는지 확인

4. **단계별 테스트**
   - 게임을 실행했을 때 화면에 무엇이 보이는지 확인
   - 버튼을 클릭했을 때 반응이 있는지 확인
   - Console에 어떤 메시지가 나오는지 확인

## 💡 해결 방법

가장 가능성이 높은 문제는 **Inspector에서 참조가 할당되지 않은 것**입니다.

1. Unity 에디터에서 `QuizManager`가 붙은 GameObject 선택
2. Inspector 창 확인
3. 모든 Public 필드에 적절한 GameObject/컴포넌트 할당
4. 특히 `quizzes` 배열에 퀴즈 데이터 추가
5. `optionButtons` 배열에 3개의 버튼 할당

만약 에러가 계속 발생한다면, Unity Console의 정확한 에러 메시지를 알려주세요!

